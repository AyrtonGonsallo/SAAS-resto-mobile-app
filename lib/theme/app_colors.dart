import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF43B9B2);  // rouge
  static const secondary = Color(0xFFC280D2); // or
  static const dark = Color(0xFF3D3D47);      // gris très foncé
  static const light = Color(0xFFF8F8F8);     // fond clair
  static const tertiary = Color(0xFFFD7E40);     // fond clair
  static const black = Color(0xFF000000);     // fond clair
  static const success = Color(0xFFA5DC86);     // fond clair
  static const failure = Color(0xFFC42A02);     // fond clair
  static const danger = Color(0xFFC42A02);
  static const info = Color(0xFF2e8dd3); // fond clair
}